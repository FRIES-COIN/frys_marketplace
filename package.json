{
    "engines": {
        "node": ">=16.0.0",
        "npm": ">=7.0.0"
    },
    "name": "frys_marketplace",
    "scripts": {
        "prebuild": "cd src/frys_marketplace_frontend && npm run generate",
        "build": "npm run build --workspaces --if-present",
        "start": "npm start --workspaces --if-present",
        "test": "npm test --workspaces --if-present"
    },
    "type": "module",
    "workspaces": [
        "src/frys_marketplace_frontend"
    ]
}